// import SearchBar from "../components/SearchBar";
// import { render, fireEvent } from "@testing-library/react";

// describe("<SearchBar />", () => {
//   test("triggers handleChange function onChange", () => {
//     const handleTextInputChange = jest.fn();
//     const wrapperRender = render(
//       <SearchBar handleTextInputChange={handleTextInputChange} />
//     );
//     const input = wrapperRender.getByTestId("searchbar");
//     fireEvent.change(input, { target: { value: "Luke" } });
//     expect(handleTextInputChange).toHaveBeenCalled();
//   });
// });
export const fn = () => {};
